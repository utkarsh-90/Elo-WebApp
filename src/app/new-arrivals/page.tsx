import CategoryPage from '@/components/CategoryPage';

export default function NewArrivalsPage() {
    return (
        <CategoryPage
            title="New & Trending"
            filterMode="new"
            description="Fresh drops and the latest styles. Be the first to rock the newest releases."
        />
    );
}
